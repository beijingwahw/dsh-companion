# Changelog

本项目的所有显著变更记录于此文件。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.4.0] — 2026-09-20

七模块全面升级：A–G 每个模块获得一项世界级能力提升，全部纯本地、确定性、可测。

### 模块 A · 隐私脱敏 v2

- 新增三类打码：**IPv4 地址**（保留前两段；`v` 前缀版本号与超 255 段防误报）、**API 密钥**（`sk-` / `ghp_` / `AKIA` / `xox*-` 等已知前缀，保留前缀掩码其余）、**JWT / Bearer 令牌**（整体掩码，JWT 先于 Bearer 处理避免重复计数）。`RedactionStats` 新增 `ipv4` / `secret` 字段。

### 模块 B · 摘要质量体检

- 新增 `modules/handoff/quality.js`：交接摘要生成后附纯本地体检报告——词元覆盖率（源转录头部显著词 + `missingTerms` 漏词清单，角色标签剔除）、四段式结构完整、长度纪律（500 字预算）、压缩充分（≥3×；短源中立）。verdict strong/fair/weak，与生成模型无关（同一把尺子横向比较）。`POST /handoff/generate` 响应新增 `quality` 字段（单发与 map-reduce 两路均附）。

### 模块 C · 节能顾问

- 新增 `modules/cost/advice.js` + `GET /cost/advice`：把用量数据翻译成按严重度与节省金额排序的行动建议——预算刹车（月末投影超支 → 剩余天数摊平日预算）、峰谷迁移节省估算、缓存命中率 +10 点潜力、同厂商模型迁移候选。快照由用量表 + forecast 引擎 + 当前价表组装；零副作用，采纳与否由用户决定。

### 模块 D · 自然语言时间过滤

- 新增 `modules/search/timeRange.js`：`GET /search` 接受 `range` 参数——`近7天` / `昨天` / `上周`（周一起点）/ `本月` / `last week` 等中英表达直接翻成北京时间自然日对齐的闭区间；仅在未显式给 from/to 时生效，未识别静默降级。

### 模块 E · 负向词排除

- 新增 `core/retrieval/negate.js` 并接线检索管线：`-词` 修饰符（`docker 网络 -k8s`）把含排除词元的文档在排序前整体出局——排除是硬约束不是降权；判定与索引同源分词（中文排除词天然支持），响应附 `negations` 溯源字段；零命中救援与盲区记录均使用剥离后的查询。

### 模块 F · 回声脉搏

- 主动脉搏（轴线 18）接入回声雷达（轴线 33）信号源：`echo` 分类洞察卡——复发 ≥3 次主题 → watch 级（"总在遗忘后重新发现"）+ 固化模板行动指引；无固化级主题时回声率 ≥20% 才 info 级知会；样本 <8 或无回声静默。推送克制纪律不变。

### 模块 G · 研究预演

- 新增 `POST /synthesis/preview`（零 LLM）：证据收集管线（召回 → 块级检索 → 次模选择）抽为 `collectEvidence` 供 answer/preview 共用；预演返回将采用的证据（编号与合成 `[n]` 引用一致）、方面覆盖、字符/token 预算占用——先看证据再花钱合成。证据为空返回引导而非报错。

### 测试

- 新增 `test/module-upgrades.mjs`（17 项断言，覆盖六个模块升级的纯函数核心）；隐私旧断言随 stats 新字段更新。累计 **11 套件 / 226 项断言**。

## [0.3.0] — 2026-09-20

「知识宇宙学三部曲」发布：认知架构从 31 条轴线扩展至 **34 条**，三块新引擎全部纯本地、零 LLM、零网络、确定性可测。

### 新增（轴线 32 · 知识考古 / 板块构造学）

- **引擎** `src/core/cognition/archaeology.ts`：会话流按 index 均分为 N 个「纪元」（缺省 4，∈ [2, 12]），每纪元独立构建实体共现图并跑轴线 26 同源 Louvain（参数一致保证跨纪元可比），相邻纪元社区按成员实体键集合 Jaccard 建立血脉（阈值 0.3），血脉结构分类为五类**板块事件**——新生 / 延续（附成员重叠率）/ 分裂 / 合并 / 消亡。轴线 26 回答"版图现在长什么样"，轴线 32 回答"这块版图是怎么长出来的"；与轴线 30 分工——漂移看会话级注意力切换（浪），考古看纪元级知识域结构变迁（洋流）。
- **端点** `GET /knowledge/archaeology?windows=4`；**命令** `archaeology`。

### 新增（轴线 33 · 回声雷达 / 会话级复发检测）

- **引擎** `src/core/cognition/radar.ts`：复用轴线 31 同源 MinHash-LSH 基础设施于**会话粒度**（文本投影 = 标题 + 实体名，零转录重读；阈值 0.55），回答片段级折叠看不见的宏观重复——**回声簇**（互为近重复的会话）、**复发周期**（首末间隔 / (次数−1)——"你平均每 12.3 天重新问一次"；周期短于遗忘阶梯 = 总在遗忘后重新发现）、**回声率**（近 N 天新会话命中历史回声的占比）。行动分层：复发 ≥3 次 → 建议固化为交接模板（直连模块 B）；2 次 → 关注。
- **端点** `GET /knowledge/radar?days=30`；**命令** `radar`。

### 新增（轴线 34 · 知识暗物质 / 缺失连接预测）

- **引擎** `src/core/cognition/darkmatter.ts`：实体共现图上的**链路预测**（Liben-Nowell & Kleinberg 2003 局部指数族）——对每个未连接且存在共同邻居的实体对计算 **CN / Adamic-Adar / Resource Allocation** 三指数（枢纽度数折扣：经过低度专业桥的"认识"才是强证据），融合分 = 0.5×AA 归一 + 0.5×RA 归一；候选对按桥节点展开天然跳过无邻居对，枢纽（度 > 40）不作桥证据，预算熔断防御病态稠密图。暗连接 = **该连而未连的知识对**——你的历史暗示相关、却从未有一场对话同时谈到的暗区；与轴线 27 分工：星图沿已有边扩散（已知连接深挖），暗物质预测缺失边（未知连接发现）。
- **端点** `GET /knowledge/darkmatter?limit=20`；**命令** `darkmatter`。

### 测试与文档

- 新增冒烟套件 `test/axes-32-33.mjs`（14 项）与 `test/axes-34.mjs`（6 项）：板块事件五类分类、回声簇 / 复发周期 / 回声率 / 行动分层、CN/AA/RA 三指数 / 度数折扣 / 已连对排除，全部含确定性与边界守卫断言。累计 **10 套件 / 209 项断言**。
- 双语 README、DESIGN.md 契约（模块 F 端点表补齐轴线 26–34 全部条目与命令表）同步更新。

## [0.2.0] — 2026-09-20

工程化加固发布：代码零变更，聚焦开发体验、测试覆盖与项目元数据一致性。

### 新增

- **统一测试入口**：`pnpm test` 一键执行全部冒烟套件（`test/run-all.mjs` 顺序驱动、汇总断言数、失败即非零退出）。
- **核心原语测试套件**（`test/core-primitives.mjs`，28 项断言）：隐私脱敏（手机/邮箱/身份证/银行卡 + 分组归一化 + 位宽空窗区无误报）、AES-256-GCM（往返 + 随机 IV + 篡改检测 + 密钥守卫）、零依赖 ZIP（结构 + 目录穿越中和）、PDF（Latin-1 判定 + 文档结构）、北京时间峰谷窗口（边界 + 跨午夜环绕）、会话转录（日志派生 + 内容压平）、计价换算（命中拆分 + 钳制）。模块 A–D 依赖的底层工具首次获得回归保护。
- **CI 工作流**（`.github/workflows/ci.yml`）：Node 22 / 24 矩阵，pnpm 冻结锁安装 → 类型检查 → 构建 → 全部测试。
- **社区与工程文件**：`CHANGELOG.md`、`CONTRIBUTING.md`、`.editorconfig`。

### 变更

- 测试文件从仓库根目录（`smoke-axes-*.mjs`）迁入 `test/`（`axes-*.mjs`），根目录保持整洁。
- `package.json`：描述补全至七大模块；新增 `keywords` / `repository` / `homepage` / `bugs` / `engines`（`^22.19 || >=24`，与 README 前置要求一致）；新增 `test` 脚本。
- `dsh.plugin.json`：描述与 `manifest.json` 对齐；模块声明补全 `knowledge`（对话知识资产）与 `synthesis`（跨会话知识合成）——此前缺失导致两份声明不一致。
- 三处版本号（`package.json` / `dsh.plugin.json` / `manifest.json`）统一升为 `0.2.0`。
- 双语 README 同步：测试计数更新为 8 套件 189 项断言；英文版滞留的「78 项断言（四套套件）」修正。

### 移除

- 误用 `npm install` 残留的 `package-lock.json`（本项目以 pnpm 为唯一包管理器，双锁文件存在依赖漂移风险）。

## [0.1.0] — 初始发布

DeepSeek Harness 官方伴侣插件首个公开版本：A–G 七大模块（对话智能导出 / 上下文交接摘要 / API 成本优化 / 全局对话检索 / 本地语义检索 / 对话知识资产 / 跨会话知识合成），认知架构 3.0（轴线 1–31），累计 161 项算法冒烟断言。
